(function(){
    window.onload = () => {
        const form = document.forms.lions;
        form.addEventListener('submit', () => {
            const formData = new FormData(form);

        });
    };
})();